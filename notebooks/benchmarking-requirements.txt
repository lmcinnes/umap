# Latest package set used to regenerate notebooks/benchmarking.ipynb.
# Install this repository separately in editable mode so the benchmark uses
# the current checkout:
#   uv venv --python 3.12 .benchmark-latest-venv
#   uv pip install --python .benchmark-latest-venv/bin/python -e . \
#       -r notebooks/benchmarking-requirements.txt
numpy==2.5.2
scipy==1.18.0
scikit-learn==1.9.0
numba==0.67.0
pynndescent==0.6.0
openTSNE==1.0.4
pandas==3.0.5
matplotlib==3.11.1
seaborn==0.13.2
jupyter==1.1.1
